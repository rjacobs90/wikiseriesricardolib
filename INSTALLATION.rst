============
Installation
============

At the command line::

    $ pip install wikiseriesricardolib

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv wikiseriesricardolib
    $ pip install wikiseriesricardolib

Or, if you are using pipenv::

    $ pipenv install wikiseriesricardolib

